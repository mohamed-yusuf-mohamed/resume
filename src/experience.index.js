import Experience from "./experience"
import styled from "styled-components"
const experience = styled(Experience)`
& ul {
  padding: 0px;
}
& li {
  list-style-type: none;
}
`
export default experience